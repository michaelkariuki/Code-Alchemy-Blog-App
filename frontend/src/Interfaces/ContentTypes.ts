import { BlogListItemProps } from "../Components/BlogListItem"
import { SidePanelWrapperProps } from "./BlogTypes"

type ProfileProps = {
  primary? : BlogListItemProps[]
  sidepanel? : SidePanelWrapperProps[]
}

type ReadingListsProps = {
    
}

