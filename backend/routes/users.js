var express = require('express');
var router = express.Router();


/* GET users listing. */
// router.get('/profile/:userId', async (req, res) => {
//   //Use userId to get profile info (User and Profile Tables)

// });

module.exports = router;
